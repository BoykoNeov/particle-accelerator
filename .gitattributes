# Normalise line endings: store text as LF in the repo, regardless of platform.
* text=auto eol=lf

# Binary assets should never be normalised.
*.png binary
*.jpg binary
*.pdf binary
*.npy binary
*.npz binary
